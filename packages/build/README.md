# ts-morph-build
A Rollup Replacement it offers a rollup-plugin-ts-build to get used in existing rollup builds. It has also a own rollup compatible module to reuse your plugins.
But rollup is total optional this works more great Standalone.
